var queue = require('../../workers/queue');
var models = require("../../../models");
var i18n = require('../../utils/i18n');

module.exports = function (notification, user, callback) {
  // See if news item for same notification exists and set updated time if needed

  // Create news item from notification

  // If notification is from somebody I follow include it for all levels (domain, community, group) in one write

  // If notification passes (domain/community/group) recommendation tops 20 test include it in individual newsfeed for the user


};
