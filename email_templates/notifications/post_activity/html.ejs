<% include ../header.html.ejs %>

<% if (activity.type == 'activity.post.new') { %>
  <h1><%= post.id == user.id ? t('notification.email.post.new') : t('notification.email.new_post_self') %></h1>
<% } if (activity.type == 'activity.post.endorsement.new') { %>
  <h1><%= post.id == user.id ? t('notification.email.post.endorsement') : t('notification.email.post.endorsement_self') %></h1>
<% } if (activity.type == 'activity.post.opposition.new') { %>
  <h1><%= post.id == user.id ? t('notification.email.post.opposition') : t('notification.email.post.opposition_self') %></h1>
<% } %>

<%= "https://"+community.hostname+"."+domain.domain_name+"/#!/post/"+post.id %>

<% include ../footer.html.ejs %>
