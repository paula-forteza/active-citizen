<% include ../header.text.ejs %>

<% if (activity.type == 'activity.post.new') { %>
  <p><%= post.id == user.id ? t('notification.email.new_post') : t('notification.email.newPostSelf') %></p>
<% } if (activity.type == 'activity.post.endorsement.new') { %>
  <p><%= post.id == user.id ? t('notification.email.post.endorsement') : t('notification.email.postEndorsementSelf') %></p>
<% } if (activity.type == 'activity.post.opposition.new') { %>
  <p><%= post.id == user.id ? t('notification.email.post.opposition') : t('notification.email.postOppositionSelf') %></p>
<% } %>

<%= "https://"+community.hostname+"."+domain.domain_name+"/post/"+post.id %>

<% include ../footer.text.ejs %>
