<% include ../header.html.ejs %>

<% if (activity.type == 'activity.point.new') { %>
  <p><%= post.id == user.id ? t('notification.email.pointNewSelf') : t('notification.email.pointNew') %></p>
<% } %>
<% if (activity.type == 'activity.point.unhelpful') { %>
  <p><%= post.id == user.id ? t('notification.email.pointUnhelpfulSelf') : t('notification.email.pointUnhelpful') %></p>
<% } %>
<% if (activity.type == 'activity.point.helpful') { %>
  <p><%= post.id == user.id ? t('notification.email.pointHelpfulSelf') : t('notification.email.pointHelpful') %></p>
<% } %>

<%= "https://"+community.hostname+"."+domain.domain_name+"/#!/post/"+post.id+"/"+point.id %>

<% include ../footer.html.ejs %>
